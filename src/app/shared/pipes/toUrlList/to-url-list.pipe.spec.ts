import { ToUrlListPipe } from './to-url-list.pipe';

describe('ToUrlListPipe', () => {
  it('create an instance', () => {
    const pipe = new ToUrlListPipe();
    expect(pipe).toBeTruthy();
  });
});

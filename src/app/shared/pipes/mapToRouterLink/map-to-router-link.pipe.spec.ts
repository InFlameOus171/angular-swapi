import { MapToRouterLinkPipe } from './map-to-router-link.pipe';

describe('MapToRouterLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new MapToRouterLinkPipe();
    expect(pipe).toBeTruthy();
  });
});

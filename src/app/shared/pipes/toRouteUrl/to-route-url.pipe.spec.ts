import { ToRouteUrlPipe } from './to-route-url.pipe';

describe('ToRouteUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new ToRouteUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
